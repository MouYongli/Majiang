dev:
	uvicorn app.main:app --reload

test:
	pytest

clean:
	rm -rf .pytest_cache
	rm -rf __pycache__
	rm -rf .DS_Store
	rm -rf .env
	rm -rf .venv
	rm -rf .mypy_cache
	rm -rf *.egg-info
	rm -rf build
	rm -rf dist
	rm -rf .ruff_cache
	rm -rf .mypy_cache
	rm -rf .pytest_cache
	rm -rf .ruff_cache